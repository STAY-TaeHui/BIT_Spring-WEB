package DI2;

public class Program {

	public static void main(String[] args) {
		
		NewRecordView view = new NewRecordView();
		
		//놀다가 NewRecord객체가 필요하다면 생성
		NewRecord record = new NewRecord(100, 50, 50);
		view.setRecord(record); // 주입(의존성) : setter
		view.print();
		
		
		
	}
}
